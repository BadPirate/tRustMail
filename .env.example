# Database configuration
DATABASE_URL=postgres://postgres:password@localhost:5432/mailsender
PGHOST=localhost
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=mailsender
PGPORT=5432

# Configuration file path (optional, defaults to config.yaml)
CONFIG_PATH=config.yaml

# Logging level (optional)
RUST_LOG=info
